# AI Design Assistant

> AI 기반 디자인 분석 및 개선 시스템  
> 텍스트로 입력한 디자인 의도와 실제 이미지를 비교 분석하고, 전문적인 피드백과 개선안을 자동으로 생성합니다.

---

## 프로젝트 소개

본 프로젝트는 사용자가 자신의 디자인이 의도한 대로 구현되었는지 객관적으로 평가할 수 있도록 돕는 AI 도구입니다.

### 해결하고자 한 문제

- 🙄 "내 디자인이 의도한 느낌을 제대로 전달하고 있을까?"
- 🤔 "어떤 부분을 개선해야 더 나은 디자인이 될까?"
- 😭 "전문 디자이너의 피드백을 받고 싶지만 시간과 비용이 부담스러워..."

### 제공하는 솔루션

1. **자동 디자인 분석**: 이미지에서 주요 디자인 요소를 자동으로 식별
2. **의도 정렬 점수**: 텍스트로 입력한 디자인 의도와 실제 구현의 일치도를 0-100점으로 정량화
3. **전문가 수준 피드백**: 디자인 이론에 기반한 구체적이고 실행 가능한 개선 제안
4. **AI 기반 개선안 생성**: DALL-E 3를 활용한 자동 디자인 개선안 생성
5. **비교 리포트**: 원본과 개선안을 나란히 비교할 수 있는 시각적 리포트

---

## 주요 기능

### 1. 이미지 분석 (SAM2 개념)

```
입력: 디자인 이미지 (PNG, JPG 등)
출력: 주요 디자인 요소 세그먼테이션
- 헤더, 버튼, 텍스트 블록, 이미지 등 자동 식별
- 각 요소의 색상, 타이포그래피, 레이아웃 특징 추출
```

### 2. 유사도 점수 계산 (CLIP 기반)

```
입력: 이미지 + 텍스트 의도
출력: 다차원 유사도 점수
- 전체 점수 (Overall Score)
- 시각적 정렬 (Visual Alignment)
- 의미적 정렬 (Semantic Alignment)
- 세부 점수 (색상, 레이아웃, 스타일 매치)
```

### 3. 전문가 피드백 (RAG + CoT)

```
입력: 분석 결과 + 유사도 점수
출력: 구조화된 전문가 피드백
- 강점 (Strengths)
- 개선 영역 (Areas for Improvement)
- 구체적 권장사항 (Concrete Recommendations)
- 디자인 이론 적용 (Theory Application)
```

### 4. 개선안 생성 (DALL-E 3)

```
입력: 원본 이미지 + 피드백
출력: AI가 생성한 개선된 디자인
- 피드백을 반영한 새로운 디자인
- 원본과 개선안 나란히 비교 이미지
```

---

## 기술 스택

### AI/ML 모델

| 모델 | 용도 | 구현 방식 |
|------|------|-----------|
| **GPT-4o** | 이미지 분석, 피드백 생성 | OpenAI API |
| **SAM2** | 세그먼테이션 | GPT-4V로 개념 구현 |
| **CLIP** | 이미지-텍스트 유사도 | GPT-4V로 개념 구현 |
| **DALL-E 3** | 이미지 생성 | OpenAI API |

### 핵심 기술

- **RAG (Retrieval-Augmented Generation)**: 디자인 가이드라인 지식 베이스 활용
- **CoT (Chain-of-Thought)**: 논리적인 단계별 피드백 생성
- **Late Fusion**: 시각적/의미적 특징 독립 추출 후 통합

### 개발 환경

- Python 3.13.3
- OpenAI API (GPT-4o, DALL-E 3)
- PIL (Pillow) - 이미지 처리
- NumPy - 수치 계산

---

## 설치 방법

### 1. 저장소 클론

```bash
git clone https://github.com/yourusername/ai-design-assistant.git
cd ai-design-assistant
```

### 2. 필수 패키지 설치

```bash
pip install numpy pillow openai requests python-dotenv
```

**또는 requirements.txt 사용**:

```bash
pip install -r requirements.txt
```

### 3. OpenAI API 키 설정

프로젝트 루트에 `.env` 파일 생성:

```bash
OPENAI_API_KEY=sk-proj-your-api-key-here
```

> **API 키 발급**: [OpenAI Platform](https://platform.openai.com/api-keys)에서 생성

### 4. 환경 확인

```bash
python test_setup.py
```

모든 항목에 ✓가 표시되면 설정 완료.

---

## 사용 방법

### 기본 사용법

```bash
python main.py -i [이미지 경로] -t "[디자인 의도]"
```

### 주요 옵션

| 옵션 | 설명 | 예제 |
|------|------|------|
| `-i`, `--image` | 분석할 이미지 경로 (필수) | `-i design.png` |
| `-t`, `--intent` | 디자인 의도 텍스트 (필수) | `-t "Modern landing page"` |
| `--no-generation` | 이미지 생성 건너뛰기 (빠름, 저비용) | `--no-generation` |
| `-o`, `--output-dir` | 출력 디렉토리 | `-o results/` |
| `--quality` | 이미지 품질 (standard/hd) | `--quality hd` |
| `--api-key` | API 키 직접 전달 | `--api-key sk-...` |

---

## 실행 예제

### 예제 1: 빠른 분석 (이미지 생성 제외)

```bash
python main.py -i mydesign.png -t "Modern SaaS landing page" --no-generation
```

**출력**:
- ✅ 디자인 요소 분석
- ✅ 유사도 점수 (0-100)
- ✅ 전문가 피드백
- ✅ 텍스트 리포트

### 예제 2: 전체 파이프라인 (개선안 포함)

```bash
python main.py -i mydesign.png -t "Minimalist portfolio website with dark theme"
```

**출력**:
- ✅ 위의 모든 기능 +
- ✅ AI 생성 개선안 이미지
- ✅ 원본 vs 개선안 비교 이미지

### 예제 3: 고품질 이미지 생성

```bash
python main.py -i mydesign.png -t "Professional corporate website" --quality hd
```

### 예제 4: 사용자 정의 출력 디렉토리

```bash
python main.py -i mydesign.png -t "E-commerce product page" -o my_results/
```

### 예제 5: 인자 없이 실행 (자동 데모)

```bash
python main.py
```

테스트 이미지가 자동 생성되고 전체 파이프라인이 실행됩니다.

---

## 출력 파일

실행 후 `output/` 디렉토리에 다음 파일들이 생성됩니다:

```
output/
├── analysis_report.txt          # 전체 분석 리포트
├── [원본파일명]_improved.png    # AI 생성 개선안
└── [원본파일명]_comparison.png  # 원본 vs 개선안 비교
```

### 리포트 내용

```
============================================================
  DesignIntent AI - Analysis Report
============================================================

Timestamp: 2025-01-07T15:30:00
Image: mydesign.png
Intent: Modern SaaS landing page

------------------------------------------------------------
SIMILARITY SCORES
------------------------------------------------------------
Overall Score: 75.50/100
Visual Alignment: 72.00/100
Semantic Alignment: 80.00/100

Detailed Breakdown:
  • Color Match: 70.00/100
  • Layout Match: 75.00/100
  • Style Match: 73.00/100

------------------------------------------------------------
DESIGN ANALYSIS
------------------------------------------------------------
Detected Elements: Navigation bar, Hero section, CTA button, ...

Composition: The design follows a centered layout with good ...

------------------------------------------------------------
EXPERT FEEDBACK
------------------------------------------------------------
[구체적인 피드백 내용]

**Strengths:**
- 명확한 시각적 계층 구조
- 일관된 색상 팔레트 사용
...

**Areas for Improvement:**
- 타이포그래피 크기 조정 필요
- 여백 공간 증대 권장
...

**Concrete Recommendations:**
1. 헤딩 폰트 크기를 48px로 증가
2. 버튼 대비를 4.5:1 이상으로 조정
...

------------------------------------------------------------
GENERATED OUTPUTS
------------------------------------------------------------
Improved Design: output/mydesign_improved.png
Comparison Image: output/mydesign_comparison.png
```

---

## 프로젝트 구조

```
ai-design-assistant/
├── main.py                   # 메인 실행 파일
├── test_setup.py             # 환경 진단 스크립트
├── .env                      # API 키 설정 (생성 필요)
├── requirements.txt          # 패키지 의존성
│
├── core/
│   ├── __init__.py
│   ├── analyzer.py          # 이미지 분석 (SAM2 개념)
│   ├── scorer.py            # 유사도 계산 (CLIP 기반)
│   ├── feedback.py          # 피드백 생성 (RAG + CoT)
│   └── generator.py         # 이미지 생성 (DALL-E 3)
│
├── utils/
│   ├── __init__.py
│   ├── image_utils.py       # 이미지 처리 도구
│   ├── text_utils.py        # 텍스트 처리 도구
│   └── path_utils.py        # 파일 경로 관리
│
├── data/                     # 지식 베이스
│   ├── design_principles.txt # 디자인 원칙
│   └── ux_guidelines.txt     # UX 가이드라인
│
├── output/                   # 출력 디렉토리 (자동 생성)
│   ├── analysis_report.txt
│   ├── *_improved.png
│   └── *_comparison.png
│
└── test_samples/             # 테스트 이미지 (자동 생성)
    └── test_design.png
```

### 핵심 모듈 설명

#### 1. `analyzer.py` - 이미지 분석기
```python
class DesignAnalyzer:
    """
    SAM2 개념을 적용한 디자인 요소 세그먼테이션
    GPT-4V를 활용하여 이미지에서 주요 디자인 요소 추출
    """
    def analyze_image(image_path: str) -> Dict
```

#### 2. `scorer.py` - 유사도 점수 계산
```python
class SimilarityScorer:
    """
    CLIP 개념 기반 이미지-텍스트 정렬 점수 계산
    Late Fusion 방식으로 시각적/의미적 특징 평가
    """
    def calculate_similarity(image_path: str, text_intent: str) -> Dict
```

#### 3. `feedback.py` - 피드백 생성기
```python
class FeedbackGenerator:
    """
    RAG + CoT 방식의 전문가 수준 피드백 생성
    디자인 가이드라인 지식 베이스 활용
    """
    def generate_feedback(text_intent: str, analysis: Dict, scores: Dict) -> str
```

#### 4. `generator.py` - 이미지 생성기
```python
class DesignGenerator:
    """
    DALL-E 3 기반 개선된 디자인 생성
    피드백을 반영한 새로운 디자인 제안
    """
    def generate_improved_design(...) -> str
```

---
